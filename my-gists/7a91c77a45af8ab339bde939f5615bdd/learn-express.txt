// app.get('/api/notes', (req, res) => {
// res.json(notes);
// });

// app.get('/api/notes/:id', (req, res) => {
// const note = notes.find((n) => n.\_id === req.params.id);
// res.send(note);
// });

// app.use(exportError);
// app.use(notFound);
// app.use(errorHandler);

from server.ts of note zipper
